import Hero from "../components/hero/Hero";

export default function Home() {
  return (
    <>
      <Hero />
      {/* Services / Facilities / Contact sections can be added here as
          separate components later, following the same pattern as Hero. */}
    </>
  );
}
